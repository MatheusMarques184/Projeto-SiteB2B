import React from "react";
import * as S from './styles'


export default function GetStarted():JSX.Element {

  return (
    <S.GetStarted>
      <S.GetStartedContent>
        <S.GetStartedTitle>Get started today!</S.GetStartedTitle>
        <S.GetStartedLink>PICK A PLAN</S.GetStartedLink>
      </S.GetStartedContent>
    </S.GetStarted>
  )
}
